import React, {Component} from 'react';
import { Text, View, StyleSheet, Button } from 'react-native';

const Titulo = (props) => {
  return (
    <View>
      <Text>{this.props.titulo}</Text></View>)
  }

export default Titulo;